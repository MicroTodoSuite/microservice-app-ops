# Terraform working files, state, plans, and variable files. A module commits
# no lock file (PC-IAC-006); only sample/terraform.tfvars is committed.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
tfplan*
*.tfvars
!sample/terraform.tfvars
