# Tasks: Naming-Convention Adoption and Infrastructure Rebuild

Traceability: each task names its artifact. Tasks marked **[approval]** need the
maintainer's approval of an exact saved plan or operation before they run.

## Phase 0: Preconditions

- [X] T001 Record the deployed inventory with current and new names in `specs/004-naming-convention-rebuild/plan.md`
- [X] T002 Merge the constitution 4.0.0 amendment permitting an approved rebuild (maintainer) — approved by the maintainer on 2026-09-11 and merged as microservice-app-docs#22
- [X] T003 Amend spec 009 FR-003 and user story 5 in `microservice-app-gitops` to match the constitution (maintainer approval) — delivered in gitops#108, merged 2026-09-11 with the approval of Tiago0507: spec 009's `**Amended**: 2026-09-11` note on `main` brings FR-003, user story 5, FR-045, FR-046, the active-active assumption, and the scope boundary into line with constitution 4.0.0 and ADR-0001; that teammate approval was accepted as this gate on 2026-09-14 by the maintainer's delegate, at the maintainer's request (microservice-app-docs `docs/Governance and IaC standards program.md` §11, decisions delegated on 2026-09-14)

## Phase 1: Preserve

- [X] T004 Archive every object in the old state bucket and every local backup to `~/backups-microtodosuite/rebuild-<timestamp>/`, with checksums — delivered 2026-09-14 in `~/backups-microtodosuite/rebuild-20260914T045432Z/`: all 26 object versions of the old state bucket with its version listing (which also records its 12 delete markers) and versioning status, the 18 earlier local backups, and the hosted zone export; `SHA256SUMS` covers the 49 files and verified after writing
- [ ] T005 Snapshot the four observability volumes and record the snapshot IDs — **not delivered**: the volumes were deleted by the 2026-09-11 lifecycle teardown before a snapshot existed; the loss is recorded in `plan.md`
- [ ] T006 Export both log groups and the Route 53 record sets — partial: the Route 53 record sets are exported (2026-09-14, `~/backups-microtodosuite/rebuild-20260914T045432Z/route53/`: `microtodosuite.abrdns.com` holds only its NS and SOA records); the log groups were deleted with the runtime on 2026-09-11 and cannot be exported, so this part is **not deliverable**
- [ ] T007 Record the live ArgoCD revision and Application health as the pre-rebuild baseline — **not delivered**: no cluster remained; the last reconciled GitOps revision is the quiescence commit of microservice-app-gitops#113

## Phase 2: Build (no destruction)

- [X] T008 Add failing contracts for the naming, tag, domain, and module-source rules against the plan JSON in `tests/contract/` — delivered with six passing domain fixtures and four rejected mutations; the reviewed `security-irsa` pass maps to the security plan domain, and the real-root gate remains red by design until T015 supplies the approved plan JSON
- [X] T009 Decompose `environment-foundation` into the modules of the target layout, each with the PC-IAC-001 layout, `sample/`, and `tests/` — delivered: thirteen modules in `terraform-aws-modules`, each with the PC-IAC-001 files, `sample/`, `tests/`, and a release tag (`ecr-repository`, `eks-cluster` v1.1.0, `eks-node-group`, `iam-oidc-provider`, `iam-role`, `kms-key`, `network`, `route53-zone`, `secret`, `security-group`, `state-backend`, `transit-egress`), cover every resource the rebuilt roots create, and `karpenter-interruption` (terraform-aws-modules#37, #38, released as `karpenter-interruption-v1.0.0` on 2026-09-14) covers the last of them: the interruption queue, its policy, and the five EventBridge rules. The legacy module's remaining opt-in Karpenter prerequisites are root work, not module work, and landed in T030: the queue takes SQS-managed encryption rather than a key of its own, the controller's identity is an IRSA role in each environment's IRSA pass, and the nodes reuse the bootstrap group's node role (gitops spec 009 T086)
- [X] T010 Write the `shd` roots, with the GitHub OIDC provider and the public zone imported rather than created — delivered 2026-09-14: `shd/state` (microservice-app-ops#66), `shd/security` with the GitHub OIDC provider's `import` block (#67) and the Terraform deploy role (#80), `shd/registry` (#68), and `shd/dns` with the public zone's `import` block (#69) are on `main`, each with tests and a lock file, and the `iac-checks` push run at `4e62041` passed for `shd`. As annotated while partial: `shd/state` is written, consuming `state-backend-v1.0.0`, and the reusable IaC gate runs on `aws/environments/shd`; `shd/security` is written with the GitHub OIDC provider imported, the image publisher role, the flow-log key and role, and the Terraform deploy role `lex-mts-shd-role-tfdeploy` (named operators with MFA; `PowerUserAccess` plus IAM confined to the project's roles, reviewed managed policies, and OIDC providers; denied any change to itself); `shd/registry` is written with the five service repositories, through `ecr-repository-v1.0.0`; `shd/dns` is written with the public zone imported, through `route53-zone-v1.0.0`; the Kyverno verifier moved to each environment's IRSA pass (plan reconciliation, 2026-09-13), so the task closes when the deploy role lands
- [X] T011 Write the `eco` roots in the PC-IAC-022 domain order, consuming the account through `var.aws_account_id` — delivered 2026-09-14: the four roots are on `main`, each binding its provider with `allowed_account_ids = [var.aws_account_id]`, and the `iac-checks` push run at `c6d35cb`, on the gate pinned in microservice-app-ops#76, passed for `eco`: `eco/networking` (#70), `eco/security` (#72, #73), `eco/workload` (#74), and `eco/security-irsa` (#77, its directory the exception recorded in #75). As annotated while partial: `eco/networking` is written, consuming `network-v1.0.0` and reading shd/security's flow-log key and role by name, and the reusable IaC gate runs on `aws/environments/eco`; `eco/security` is written with the cluster and node roles, the secrets and control-plane log keys, the cluster and node security groups, the six Secrets Manager containers, and the EKS Pod Identity roles of the `vpc-cni` and `aws-ebs-csi-driver` add-ons, so the CNI is authorized before the cluster's OIDC issuer exists; `eco/workload` is written with `lex-mts-eco-eks-main` through `eks-cluster-v1.1.0`, its managed add-ons with the Pod Identity agent, and the bootstrap node group through `eks-node-group-v1.0.0`; `eco/security-irsa` is written with the cluster's IAM OIDC provider and the IRSA roles of the four JWT readers, both Slack webhook readers, Trivy Operator, and the Kyverno verifier, its directory recorded as the PC-IAC-022 exception; every `eco` root is written, and the task closes once the gate that applies that exception runs on them
- [X] T012 Restructure the `fdev`, `fstg`, `fprd`, and egress roots in code to the same layout — delivered: `shd/networking` replaces the shared egress root with the single-NAT hub through `network-v1.0.0` and `transit-egress-v1.0.0`; `fdev/networking`, `fstg/networking`, and `fprd/networking` provide their three-zone transit-only spokes; `fdev/security` plus `fdev/workload`, `fstg/security` plus `fstg/workload`, and `fprd/security` plus `fprd/workload` provide the full-profile security and workload roots. Every root has its own state key, consumes upstream resources by standard names, keeps the full-profile clusters transit-only with one bootstrap node group, and is covered by the reusable IaC gate.
- [ ] T013 Create the new ECR repositories and copy every image, signature, and attestation by digest; verify each with Cosign **[approval]**
- [ ] T014 Create the new secrets and copy both Slack webhook values without printing them **[approval]**
- [ ] T015 Produce saved plans for every new root against empty keys and make T008 pass
- [ ] T016 On a GitOps branch, replace account-dependent values with the planned outputs; update Kyverno's repository pattern and ExternalSecret keys
- [ ] T017 Update the reusable workflow's publication guard and the five service workflows to the new repository and role names

## Phase 3: Rebuild window

- [ ] T018 Pause ArgoCD auto-sync and CI publication; remove workloads and volumes through GitOps — partial: workloads were removed through GitOps quiescence (gitops#109, #112, #113); CI publication was not paused and still writes to the preserved ECR repositories
- [ ] T019 Destroy the `eco` foundation from a reviewed saved plan **[approval]** — partial: the runtime was destroyed from a reviewed lifecycle bundle on 2026-09-11; the persistent resources remain under the dev foundation state until T026
- [ ] T020 Apply the new roots in order from their saved plans **[approval]**
- [ ] T021 Merge the GitOps branch and run the audited bootstrap for `lex-mts-eco-eks-main`
- [ ] T022 Resume ArgoCD auto-sync and CI publication

## Phase 4: Validate and clean

- [ ] T023 Verify every Application Healthy and Synced and the five services' cross-service checks
- [ ] T024 Publish one image through the renamed publisher role from a service's `main`
- [ ] T025 Sweep the account for orphans and every repository for old names; record the result
- [ ] T026 Delete the old bucket, repositories, secrets, and KMS keys after the retention period **[approval]** — the default VPC moved to T028 on 2026-09-13 (decision D2 as amended in microservice-app-docs)
- [ ] T027 Activate the ops `main` ruleset requiring one approval (maintainer decision G6), once T023–T025 pass
- [X] T028 Delete the default VPC `172.31.0.0/16` in `us-east-1` before the full profile's VPCs are created, in its own approved operation and independent of T026's retention period (capacity limit L1; decision D2 as amended by the maintainer on 2026-09-13, which withdrew the VPC quota increase; ai-agents specs/001 T045) **[approval]** — precondition enforced 2026-09-14, deletion done the same day: the lifecycle refuses to start an up transition while the Region's VPCs plus the ones it would create exceed the VPCs-per-Region quota, so the full profile cannot begin while the default VPC would leave its last spoke without room (microservice-app-ops#103); the deletion was prepared the same day, with every attribute of the empty default VPC (`vpc-05c82496ca3a918da`: six subnets, `igw-0e63595c04e8ad314`, and only its default route table, network ACL, and security group) saved and checksummed under `~/backups-microtodosuite/default-vpc-20260914T201419Z/`, and the maintainer ran it: `aws ec2 describe-vpcs` in `us-east-1` now returns only `lex-mts-eco-vpc-main` (`10.10.0.0/16`)
- [X] T029 Write the IRSA pass of every full environment, after T012's workload roots — delivered 2026-09-14: `aws/environments/{fdev,fstg,fprd}/security-irsa`, each registering its cluster's OIDC issuer through `iam-oidc-provider-v1.0.0` and creating the JWT, Slack webhook, Trivy Operator, and Kyverno roles through `iam-role-v1.0.0`, with tests, a recorded PC-IAC-022 exception, and a place in the lifecycle's full profile (ai-agents specs/001 T025 and T027)
- [X] T030 Wire the Karpenter prerequisites into every full environment, after T029 — delivered 2026-09-14: `fdev/workload`, `fstg/workload`, and `fprd/workload` create the interruption queue `lex-mts-<env>-sqs-karpenter` and its five EventBridge rules through `karpenter-interruption-v1.0.0`, with a 300-second retention and SQS-managed encryption, and output the queue's name and ARN and the node role name the EC2NodeClass takes; `fdev/security-irsa`, `fstg/security-irsa`, and `fprd/security-irsa` create the controller role `lex-mts-<env>-role-karpenter`, which admits only `kube-system/karpenter` and carries the eighteen statements of Karpenter's reference CloudFormation template, each scoped to this cluster's ownership tag, this region, and this account; no Karpenter node role is created, because the nodes reuse the bootstrap group's role and its Amazon EKS access entry; the `karpenter.sh/discovery` tag the EC2NodeClass selects on was already on each environment's private subnets and node security group (ai-agents specs/001 T023, T025, and T027; gitops spec 009 T086 consumes the outputs)
- [X] T031 Create the AWS Load Balancer Controller's IRSA role in each full environment's IRSA pass, scoped to its cluster and VPC, before GitOps activates `infrastructure/aws-load-balancer-controller` (vendored at v3.5.0) on a full cluster — found 2026-09-14 while re-delivering gitops spec 009 T040 and T041: the legacy module's opt-in prerequisite has no counterpart in the rebuilt roots — delivered 2026-09-14: `fdev/security-irsa`, `fstg/security-irsa`, and `fprd/security-irsa` create `lex-mts-<env>-role-lbcontrol`, admitting only `kube-system/aws-load-balancer-controller`, with the sixteen statements of the upstream v3.5.0 IAM policy, every `elbv2.k8s.aws/cluster` tag condition requiring that environment's cluster name and every resource ARN naming its Region and account; the VPC is not a condition the upstream actions accept, so the cluster tag is the scope; GitOps annotates the service account with `irsa_role_arns["lbcontrol"]` (gitops spec 009 T083)
